import { ComplexCapiObject } from './complex-capi-object';

describe('ComplexCapiObject', () => {
  it('should create an instance', () => {
    expect(new ComplexCapiObject()).toBeTruthy();
  });
});
